[core]
    pager = delta --features "$(tmux display -p '#{COLORSCHEME_DELTA}')"

[interactive]
    diffFilter = delta --color-only --features "$(tmux display -p '#{COLORSCHEME_DELTA}')"

[delta]
    syntax-theme = ansi
    navigate = true # use n and N to move between diff

    line-numbers = true
    relative-paths = true
    conflictstyle = diff3
    side-by-side = true

[delta "lightmode"]
  light = true

[delta "darkmode"]
  light = false
