alt_send_esc: true

window:
  decorations: full

font:
  size: 17
  normal:
    family: "Iosevka Fixed"

shell:
  program: /usr/bin/env
  args:
    - bash
    - --login

key_bindings:
  - {key: Insert, mods: Shift,         action:  Paste}
  - {key: N,      mods: Alt|Shift,     action:  SpawnNewInstance}
  - {key: Space,  mods: Shift|Control, action:  None}
  - {key: F1,     mods: Alt,           command: { program: "/usr/bin/env", args: ["bash", "-lc", "tmux new-window -n HELP glow -p $PRIVATE_DOTFILES/misc/help.md"]}}
  - {key: F1,     mods: Alt|Shift,     command: { program: "/usr/bin/env", args: ["bash", "-lc", "tmux new-window -n HELP vim $PRIVATE_DOTFILES/misc/help.md"]}}
  - {key: F6,     mods: Alt,           command: { program: "/usr/bin/env", args: ["bash", "-lc", "tmux display-popup -w40% -h40% -E $HOME/.config/alacritty/bin/switch-theme.sh"]}}
  - {key: F6,     mods: Alt|Shift,     command: { program: "/usr/bin/env", args: ["bash", "-lc", "$HOME/.config/alacritty/bin/switch-theme.sh reset"]}}
    # unmap some bindings
  - {key: B,      mods: Alt,           action:  ReceiveChar}
